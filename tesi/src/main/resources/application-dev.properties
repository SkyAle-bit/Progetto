spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=secret

spring.jpa.hibernate.ddl-auto=create

# Abilita Docker Compose per sviluppo locale
spring.docker.compose.enabled=true
spring.docker.compose.file=docker-compose.yml

mail.from=${MAIL_FROM:koreadministration@gmail.com}

# Configurazione SMTP (JavaMailSender)
# Imposta le variabili d'ambiente prima di avviare il profilo dev.
<<<<<<< HEAD
spring.mail.host=${SMTP_HOST:}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH:}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS:}
=======
spring.mail.host=${SMTP_HOST}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME}
spring.mail.password=${SMTP_PASSWORD}
spring.mail.properties.mail.smtp.auth=${SMTP_AUTH}
spring.mail.properties.mail.smtp.starttls.enable=${SMTP_STARTTLS}
>>>>>>> e3ac15e04db8b7ecf07b700f6950d6db936b4f4a
spring.mail.properties.mail.smtp.connectiontimeout=${SMTP_CONNECTION_TIMEOUT:20000}
spring.mail.properties.mail.smtp.timeout=${SMTP_READ_TIMEOUT:20000}
spring.mail.properties.mail.smtp.writetimeout=${SMTP_WRITE_TIMEOUT:20000}

