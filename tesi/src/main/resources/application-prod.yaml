spring:
  datasource:
    url: jdbc:postgresql://aws-1-eu-west-3.pooler.supabase.com:6543/postgres?prepareThreshold=0&sslmode=require&binaryTransfer=false&options=-c%20statement_timeout=300000
    username: postgres.hxibvtsspedxsklsvxct
    password: yL1lO47g5L63
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 30000
      connection-timeout: 30000
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        boot.allow_jdbc_metadata_access: true

  docker:
    compose:
      enabled: false

  rabbitmq:
    host: ${RABBITMQ_HOST}
    username: ${RABBITMQ_USER}
    password: ${RABBITMQ_PASS}
    virtual-host: ${RABBITMQ_VHOST}
    port: ${RABBITMQ_PORT:5672}
    listener:
      simple:
        retry:
          enabled: true
          max-retries: 3
          initial-interval: 2000
        default-requeue-rejected: false

  mail:
    protocol: smtps
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:465}
    username: ${SMTP_USERNAME:koreadministration@gmail.com}
    password: ${SMTP_PASSWORD:scgw qype mlnj xnuz}
    properties:
      mail:
        smtps:
          auth: ${SMTP_AUTH:true}
          connectiontimeout: ${SMTP_CONNECTION_TIMEOUT:20000}
          timeout: ${SMTP_READ_TIMEOUT:20000}
          writetimeout: ${SMTP_WRITE_TIMEOUT:20000}

cors:
  allowed-origins: https://progetto-fe.vercel.app,http://localhost:4200

mail:
  from: ${MAIL_FROM:koreadministration@gmail.com}

admin:
  email: koreadministration@gmail.com

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false