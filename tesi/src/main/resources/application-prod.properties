# Supabase Transaction Pooler (Risolve definitivamente MaxClientsInSessionMode)
spring.datasource.url=jdbc:postgresql://aws-1-eu-west-3.pooler.supabase.com:6543/postgres?prepareThreshold=0&sslmode=require&binaryTransfer=false

# Credenziali separate
spring.datasource.username=postgres.hxibvtsspedxsklsvxct
spring.datasource.password=yL1lO47g5L63

# Driver e impostazioni di creazione tabelle
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configurazione HikariCP (Ottimizzata per Transaction Mode)
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000

# Disabilita Docker Compose in produzione (usa DB remoto Neon)
spring.docker.compose.enabled=false

# ?? Configurazione Email (SMTP JavaMailSender) ??
mail.from=${MAIL_FROM:koreadministration@gmail.com}
admin.email=koreadministration@gmail.com

# CORS Configuration for Production
cors.allowed-origins=https://progetto-fe.vercel.app,http://localhost:4200

# Configurazione SMTP (JavaMailSender)
spring.mail.protocol=smtps
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:465}
spring.mail.username=${SMTP_USERNAME:koreadministration@gmail.com}
spring.mail.password=${SMTP_PASSWORD:scgw qype mlnj xnuz}
spring.mail.properties.mail.smtps.auth=${SMTP_AUTH:true}
spring.mail.properties.mail.smtps.connectiontimeout=${SMTP_CONNECTION_TIMEOUT:20000}
spring.mail.properties.mail.smtps.timeout=${SMTP_READ_TIMEOUT:20000}
spring.mail.properties.mail.smtps.writetimeout=${SMTP_WRITE_TIMEOUT:20000}
