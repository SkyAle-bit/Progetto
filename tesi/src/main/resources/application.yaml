spring:
  application:
    name: tesi
  profiles:
    active: dev
  docker:
    compose:
      enabled: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

file:
  upload-dir: uploads

mail:
  from: ${MAIL_FROM:koreadministration@gmail.com}

admin:
  email: koreadministration@gmail.com

jwt:
  secret: ${JWT_SECRET:QuestaEunaChiaveSegretaMoltoLungaPerIlMioProgettoTesi12345!}
  expiration: 86400000

cors:
  allowed-origins: http://localhost:4200
